Currently this will just print out the metadata_dict
into the terminal.

The following steps to run the script in terminal:
1. uv sync
2. uv run main.py "../directory"

Please note the directory arg must be relative path.

TODO:
1. Update to accept absolute directory.
2. Change return from None to whatever is appropriate.
